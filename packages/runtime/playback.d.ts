/**
 * Playback rules for the runtime's video player — where to resume a remembered position — kept pure and
 * dependency-free so the unit gate can hold the decision. Exports the resume band (`RESUME_MIN`,
 * `RESUME_TAIL`) and `resumeAt`.
 * @module
 */
// GENERATED by tools/dts.mjs from packages/runtime/playback.js — edit the JSDoc there, never this file.
/**
 * Where to actually start playback, given a remembered position and the media duration.
 * @param saved remembered position in seconds
 * @param duration media duration in seconds (Infinity or NaN for a live stream)
 * @returns the position to seek to, or 0 to start from the top
 */
export function resumeAt(saved: any, duration: any): number;
/**
 * Playback rules for the runtime's video player — where to resume a remembered position — kept pure and
 * dependency-free so the unit gate can hold the decision. Exports the resume band (`RESUME_MIN`,
 * `RESUME_TAIL`) and `resumeAt`.
 * @module
 */
/** Seconds below which a saved position counts as not started — resume goes back to 0. */
export const RESUME_MIN: 30;
/** Fraction of the duration past which the film counts as finished — resume goes back to 0. */
export const RESUME_TAIL: 0.98;
