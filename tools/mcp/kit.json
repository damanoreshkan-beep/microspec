{
  "//": "GENERATED by tools/kit-manifest.mjs from packages/runtime/ui.js. Do not edit by hand — run the generator.",
  "source": "packages/runtime/ui.js",
  "import": "/_rt/ui.js",
  "count": 10,
  "exports": [
    {
      "name": "FROST",
      "kind": "constant",
      "line": 141,
      "headline": "Sheet — the farm's ONE bottom sheet",
      "doc": "The shell only: liquid glass (.modal-box in theme.css), drag-to-dismiss by the grip, a title row with a\nclose button, a tappable backdrop. Everything inside is the caller's — a sheet is a container, so\nstandardising it costs an app nothing and buys the farm one geometry.\n\nIt is NOT history-backed by itself, deliberately: routing is the runtime's job and the atom is the\ncaller's (S.screen for a tool app, S.sheet for the filter sheet). Pass `open`/`onClose` from that atom\nand Back closes it, per the routing invariant — a sheet that owned its own state would be the one\nscreen the system Back button couldn't reach.\n`locale` defaults to <html lang>, which the runtime keeps pointed at the UI locale — so a shared\ncomponent never has to demand a prop (or an i18n key) from every app that mounts it.\nThe box, as one exported string. The shell has four dialogs of its own (the filter sheet, the install\nmodal, the QR invite, the danger confirm) that predate this kit and each wrote their own box classes —\nwhich is how three apps ended up overflowing at 208px through a sheet none of them wrote: no width\nceiling, no min-w-0, so a control inside pushed the DOCUMENT wide. One definition, one place.\n── FROST — the material for a surface floating over a STAGE \nOver the farm's own extrusion a blur is banned (preflight: glass and the shadow pair answer the same\nquestion). Over FOREIGN content — a WebGL field, a video frame, a camera feed — the page beneath is not a\nsurface at all, and a solid base-100 panel simply hides the one thing the screen is for. Frost is the\ndetail header's recipe (render.js \"staged\"), named once: a 60% base-100 wash (deterministic contrast in\nboth themes), the default-xl blur, a 10% hairline so the edge survives a busy frame, and ONE soft cast (the\nextrusion pair would paint a light halo round glass — the light theme showed it; glass is not extruded).\nIsland / Segmented / Sheet take `tone=\"frost\"` — an app never writes the recipe itself.",
      "value": "bg-base-100/60 backdrop-blur-xl border border-base-content/10 sf-frost text-base-content"
    },
    {
      "name": "SHEET_BOX",
      "kind": "constant",
      "line": 144,
      "headline": "Sheet — the farm's ONE bottom sheet",
      "doc": "The shell only: liquid glass (.modal-box in theme.css), drag-to-dismiss by the grip, a title row with a\nclose button, a tappable backdrop. Everything inside is the caller's — a sheet is a container, so\nstandardising it costs an app nothing and buys the farm one geometry.\n\nIt is NOT history-backed by itself, deliberately: routing is the runtime's job and the atom is the\ncaller's (S.screen for a tool app, S.sheet for the filter sheet). Pass `open`/`onClose` from that atom\nand Back closes it, per the routing invariant — a sheet that owned its own state would be the one\nscreen the system Back button couldn't reach.\n`locale` defaults to <html lang>, which the runtime keeps pointed at the UI locale — so a shared\ncomponent never has to demand a prop (or an i18n key) from every app that mounts it.\nThe box, as one exported string. The shell has four dialogs of its own (the filter sheet, the install\nmodal, the QR invite, the danger confirm) that predate this kit and each wrote their own box classes —\nwhich is how three apps ended up overflowing at 208px through a sheet none of them wrote: no width\nceiling, no min-w-0, so a control inside pushed the DOCUMENT wide. One definition, one place.\n── FROST — the material for a surface floating over a STAGE \nOver the farm's own extrusion a blur is banned (preflight: glass and the shadow pair answer the same\nquestion). Over FOREIGN content — a WebGL field, a video frame, a camera feed — the page beneath is not a\nsurface at all, and a solid base-100 panel simply hides the one thing the screen is for. Frost is the\ndetail header's recipe (render.js \"staged\"), named once: a 60% base-100 wash (deterministic contrast in\nboth themes), the default-xl blur, a 10% hairline so the edge survives a busy frame, and ONE soft cast (the\nextrusion pair would paint a light halo round glass — the light theme showed it; glass is not extruded).\nIsland / Segmented / Sheet take `tone=\"frost\"` — an app never writes the recipe itself.",
      "value": "modal-box rounded-t-[1.75rem] max-w-[min(36rem,100vw)] mx-auto min-w-0 flex flex-col gap-[var(--ms-gap)] p-[var(--ms-pad)] pb-8 max-h-[88dvh]"
    },
    {
      "name": "Sheet",
      "kind": "component",
      "line": 153,
      "headline": "Sheet — the farm's ONE bottom sheet",
      "doc": "The shell only: liquid glass (.modal-box in theme.css), drag-to-dismiss by the grip, a title row with a\nclose button, a tappable backdrop. Everything inside is the caller's — a sheet is a container, so\nstandardising it costs an app nothing and buys the farm one geometry.\n\nIt is NOT history-backed by itself, deliberately: routing is the runtime's job and the atom is the\ncaller's (S.screen for a tool app, S.sheet for the filter sheet). Pass `open`/`onClose` from that atom\nand Back closes it, per the routing invariant — a sheet that owned its own state would be the one\nscreen the system Back button couldn't reach.\n`locale` defaults to <html lang>, which the runtime keeps pointed at the UI locale — so a shared\ncomponent never has to demand a prop (or an i18n key) from every app that mounts it.\nThe box, as one exported string. The shell has four dialogs of its own (the filter sheet, the install\nmodal, the QR invite, the danger confirm) that predate this kit and each wrote their own box classes —\nwhich is how three apps ended up overflowing at 208px through a sheet none of them wrote: no width\nceiling, no min-w-0, so a control inside pushed the DOCUMENT wide. One definition, one place.\n── FROST — the material for a surface floating over a STAGE \nOver the farm's own extrusion a blur is banned (preflight: glass and the shadow pair answer the same\nquestion). Over FOREIGN content — a WebGL field, a video frame, a camera feed — the page beneath is not a\nsurface at all, and a solid base-100 panel simply hides the one thing the screen is for. Frost is the\ndetail header's recipe (render.js \"staged\"), named once: a 60% base-100 wash (deterministic contrast in\nboth themes), the default-xl blur, a 10% hairline so the edge survives a busy frame, and ONE soft cast (the\nextrusion pair would paint a light halo round glass — the light theme showed it; glass is not extruded).\nIsland / Segmented / Sheet take `tone=\"frost\"` — an app never writes the recipe itself.",
      "signature": "Sheet({ id, open, onClose, title, subtitle, icon, locale, size = \"md\", tone = \"glass\", children })",
      "props": [
        {
          "name": "id"
        },
        {
          "name": "open"
        },
        {
          "name": "onClose"
        },
        {
          "name": "title"
        },
        {
          "name": "subtitle"
        },
        {
          "name": "icon"
        },
        {
          "name": "locale"
        },
        {
          "name": "size",
          "default": "\"md\""
        },
        {
          "name": "tone",
          "default": "\"glass\""
        },
        {
          "name": "children"
        }
      ]
    },
    {
      "name": "Segmented",
      "kind": "component",
      "line": 213,
      "headline": "Segmented — the farm's ONE tab / option strip",
      "doc": "One geometry, two skins, and the choice between them is semantic rather than decorative:\n  solid   — the ACTIVE option is a filled ink pill (the dock's language). Use where the choice is the\n            screen's primary mode. Contrast is a shape, not a luminance step: in a theme whose axiom is \"ink\n            is the brand\", --color-primary and --color-base-content are the same hex, so the usual\n            \"active = text-primary\" idiom resolves to 100% vs 80% of ONE colour — 1.56:1, invisible.\n            The farm's dock shipped exactly that bug for its whole life with every gate green.\n  outline — the active option is a hairline-ringed tinted pill. Use where the strip sits ON content\n            (a stage, a photo, a canvas) and a filled ink block would punch a hole in it.\nBoth are monochrome by construction. Colour enters only as `dot` — a small filled disc carrying the\napp's or the option's own hue, which is the safe place for an arbitrary colour (never text).\n\nA fitted strip DEMOTES to glyphs, it never squashes. In .ms-side's narrow column (412×430) a three-way\nstrip of labelled pills rendered \"Т… · О.. · О..\" — two options with the same visible text — and at 360×340\none letter each. The width a labelled strip needs is MEASURED (a truncated span still reports its full\nscrollWidth), and when the rail is narrower than that every label hides and the button's accessible name\nbecomes the label. Only when every option has an icon — without one, hiding the word is deletion.",
      "signature": "Segmented({ items, value, onChange, variant = \"solid\", size = \"md\", scroll = false, attr = \"data-seg\", label, tone = \"inset\" })",
      "props": [
        {
          "name": "items"
        },
        {
          "name": "value"
        },
        {
          "name": "onChange"
        },
        {
          "name": "variant",
          "default": "\"solid\""
        },
        {
          "name": "size",
          "default": "\"md\""
        },
        {
          "name": "scroll",
          "default": "false"
        },
        {
          "name": "attr",
          "default": "\"data-seg\""
        },
        {
          "name": "label"
        },
        {
          "name": "tone",
          "default": "\"inset\""
        }
      ]
    },
    {
      "name": "Island",
      "kind": "component",
      "line": 302,
      "headline": "Island — the floating glass panel",
      "doc": "The dock's material, reusable: a tool app's persistent controls are islands like it, not bars welded to\nan edge. Opacity stays high (80%) so text contrast over whatever scrolls beneath is deterministic — the\nblur does the glass. Translucency you can't predict is a contrast bug waiting for one screen to break.\n`pinned` — the island that floats between the content and the dock. Six apps had written the same\npositioner by hand (`fixed inset-x-0 z-20 flex justify-center px-3 pointer-events-none` plus a\n`bottom: calc(var(--dock-h) + env(safe-area-inset-bottom) + …)`), and they had already drifted: four used\na 0.4rem gap and two used 0.5rem, for one measurement. The POSITION and the MATERIAL are the kit's; the\nwidth, the radius and the row inside stay the app's, through className.\n`at` — which edge it clears: \"bottom\" floats above the dock, \"top\" hangs under the header. Both are the\nsame idea (a bar that clears the chrome without an app knowing the chrome's height), and both read the\nMEASURED numbers rather than restating them.\n`tone` — what it floats ON. \"glass\" is the farm's base-100 material; \"dark\" is for a bar over arbitrary\nmedia, where a light-surface island simply disappears against a bright video frame; \"frost\" (FROST below)\nis the translucent wash for a panel over the app's own STAGE (a GL field, a hero). That is a material\ndecision with a reason, not a per-app taste, which is why it belongs here.",
      "signature": "Island({ children, className = \"\", tag = \"div\", pinned = false, at = \"bottom\", tone = \"glass\", ...rest })",
      "props": [
        {
          "name": "children"
        },
        {
          "name": "className",
          "default": "\"\""
        },
        {
          "name": "tag",
          "default": "\"div\""
        },
        {
          "name": "pinned",
          "default": "false"
        },
        {
          "name": "at",
          "default": "\"bottom\""
        },
        {
          "name": "tone",
          "default": "\"glass\""
        },
        {
          "name": "...rest"
        }
      ]
    },
    {
      "name": "Panel",
      "kind": "component",
      "line": 347,
      "headline": "Panel — the solid surface",
      "doc": "What a card is in the list family: the page EXTRUDED (sf-raised at the shallow elevation), no border, an\noptional mono micro-label header. Use it for grouped controls; use Island only where the panel floats\nOVER content. It has no hairline — this comment used to say it did, and app authors copied that.",
      "signature": "Panel({ title, children, className = \"\", ...rest })",
      "props": [
        {
          "name": "title"
        },
        {
          "name": "children"
        },
        {
          "name": "className",
          "default": "\"\""
        },
        {
          "name": "...rest"
        }
      ]
    },
    {
      "name": "Slider",
      "kind": "component",
      "line": 363,
      "headline": "Slider — a labelled range",
      "doc": "The value is not printed. A macro like \"space\" or \"density\" has no unit a number would honestly carry,\nand a readout the user cannot act on is hint text with extra steps (the farm's no-hand-holding rule).",
      "signature": "Slider({ id, label, value, onInput, min = 0, max = 1, step = 0.02, attr = \"data-macro\" })",
      "props": [
        {
          "name": "id"
        },
        {
          "name": "label"
        },
        {
          "name": "value"
        },
        {
          "name": "onInput"
        },
        {
          "name": "min",
          "default": "0"
        },
        {
          "name": "max",
          "default": "1"
        },
        {
          "name": "step",
          "default": "0.02"
        },
        {
          "name": "attr",
          "default": "\"data-macro\""
        }
      ]
    },
    {
      "name": "Row",
      "kind": "component",
      "line": 380,
      "headline": "",
      "doc": "Row — the one-line flex used inside Panels/Sheets (label left, control right). Not a component so much\nas the shape they all share; exported so a caller never re-guesses the gap.",
      "signature": "Row({ children, className = \"\" })",
      "props": [
        {
          "name": "children"
        },
        {
          "name": "className",
          "default": "\"\""
        }
      ]
    },
    {
      "name": "Transport",
      "kind": "component",
      "line": 396,
      "headline": "Transport — the widget",
      "doc": "Every piece is opt-in: no `onNext` → no skip button; no `dur` → no seek bar; no `onRepeat` → no repeat\ncontrol. So a one-button ambient player and a full queue player are the same component, and a fix to the\nscrub interaction or the a11y labels lands in all of them at once.\n\nLocalisation: the runtime's SYS dictionary carries the transport strings (aPlay/aPause/aPrev/aNext/\naSeek/aRepeat…), so an app adopting this does not restate them — pass `locale`, not a dict.",
      "signature": "Transport({ locale = \"en\", playing = false, onToggle, onPrev, onNext, pos = 0, dur = 0, onSeek, repeat, onRepeat, shuffle = false, onShuffle, title, subtitle, lead, trail, stopIcon = false, size = \"md\", disabled = false, className = \"\", actions = [], keep = 2, moreOpen, onMore, onMoreClose, onScrubStart, onScrub, onScrubEnd })",
      "props": [
        {
          "name": "locale",
          "default": "\"en\"",
          "note": "SYS carries the transport strings; see i18n.js"
        },
        {
          "name": "playing",
          "default": "false"
        },
        {
          "name": "onToggle"
        },
        {
          "name": "onPrev"
        },
        {
          "name": "onNext"
        },
        {
          "name": "pos",
          "default": "0"
        },
        {
          "name": "dur",
          "default": "0"
        },
        {
          "name": "onSeek",
          "note": "seek bar appears when onSeek is given"
        },
        {
          "name": "repeat"
        },
        {
          "name": "onRepeat",
          "note": "repeat button appears when onRepeat is given"
        },
        {
          "name": "shuffle",
          "default": "false"
        },
        {
          "name": "onShuffle",
          "note": "shuffle button appears when onShuffle is given"
        },
        {
          "name": "title"
        },
        {
          "name": "subtitle",
          "note": "optional now-playing block"
        },
        {
          "name": "lead"
        },
        {
          "name": "trail",
          "note": "optional slots either side of the title row"
        },
        {
          "name": "stopIcon",
          "default": "false",
          "note": "rave-style square instead of a pause bar"
        },
        {
          "name": "size",
          "default": "\"md\"",
          "note": "\"md\" | \"sm\""
        },
        {
          "name": "disabled",
          "default": "false"
        },
        {
          "name": "className",
          "default": "\"\""
        },
        {
          "name": "actions",
          "default": "[]",
          "note": "The app's OWN controls — rave's generate, handpan's record, fmradio's power. One slot was never enough (rave's pad row carries four, handpan five), and an app that cannot express its controls through the kit keeps its hand-rolled row instead — which is how the farm ended up with six transports. { id, icon, label, onClick, active, tone: \"accent\"|\"error\", pulse, pressed, disabled, attr: {…} } `label` is the accessible name AND the word shown in the overflow sheet, so it is required."
        },
        {
          "name": "keep",
          "default": "2",
          "note": "how many actions stay inline in a narrow container"
        },
        {
          "name": "moreOpen"
        },
        {
          "name": "onMore"
        },
        {
          "name": "onMoreClose",
          "note": "history-backed overflow (S.screen) — see below"
        },
        {
          "name": "onScrubStart",
          "note": "scrub lifecycle: start (grabbed) → scrub (dragging, so the app can show the position it is heading to) → end + onSeek (committed). Apps need all three: the position readout must follow the thumb, but the engine must only be told once, on release."
        },
        {
          "name": "onScrub"
        },
        {
          "name": "onScrubEnd"
        }
      ]
    },
    {
      "name": "Stage",
      "kind": "component",
      "line": 566,
      "headline": "Stage — where a visualiser lives",
      "doc": "A full-bleed `absolute inset-0` canvas centres its subject on the SCREEN, which is the wrong centre: the\nheader sits over the top of it and the transport island over the bottom, so the object reads as pushed\nup and off-balance on every phone. A visualiser belongs in the box that is actually visible — the flex\nvoid between the chrome — and then its own geometry centring is correct for free, at every breakpoint,\nwith no measuring and no magic numbers.\n\nUse it as the `flex-1 min-h-0` void of a fit view: header · <Stage> · controls. The canvas inside is\n`absolute inset-0` relative to THIS box, not the screen.",
      "signature": "Stage({ children, className = \"\" })",
      "props": [
        {
          "name": "children"
        },
        {
          "name": "className",
          "default": "\"\""
        }
      ]
    }
  ]
}
